# <專案名稱> 開發者文件

> 這份文件給**要修改這個專案的人**。使用說明請看 [README.md](README.md)。

---

## 1. 技術棧與系統需求

| 項目 | 版本 | 用途 |
| :--- | :--- | :--- |
| <語言／執行期> | <最低版本> | <用途> |
| <建置工具> | <最低版本> | <用途> |

**作業系統限制**：<例：建置僅支援 Windows；沒有限制就寫「無」>

---

## 2. 環境建置

從一台乾淨的機器開始：

1. 取得原始碼
   ```bash
   git clone <儲存庫網址>
   ```
   <完成後應看到：…>

2. 安裝相依套件
   ```bash
   <指令>
   ```
   <完成後應看到：…>

3. <其他必要步驟>

---

## 3. 日常開發

**啟動**

```bash
<指令>
```

**修改後如何反映**：<熱更新／需重啟／需重新建置>

**除錯**：<log 位置、除錯開關、如何開啟開發者工具>

---

## 4. 目錄結構

```text
<專案根目錄>/
├─ <目錄>/          <一句話職責>
│  └─ <檔案>        <一句話職責>
├─ docs/
│  └─ rules/        通用規範（本專案遵循的文件規範）
└─ <檔案>           <一句話職責>
```

<!-- 只列到有意義的那一層。不要列 node_modules、target、dist 等產生物。 -->

---

## 5. 架構與關鍵設計決策

### 模組職責

| 模組 | 職責 | 依賴 |
| :--- | :--- | :--- |
| <模組> | <做什麼> | <依賴誰> |

### 關鍵決策

#### <決策一句話標題>

- **決定**：<做了什麼選擇>
- **理由**：<為什麼。只寫結論不寫理由等於沒寫。>
- **代價**：<放棄了什麼>

<!-- 難以反轉、日後會令人納悶的決策，另立 ADR 於 docs/adr/ 並在此連結。 -->

---

## 6. 測試

```bash
<指令>
```

| 分類 | 涵蓋範圍 | 備註 |
| :--- | :--- | :--- |
| <單元／整合／回歸> | <範圍> | <備註> |

**會被略過的測試**

<說明：哪些測試會因為缺少外部檔案而略過、那些檔案該放哪、輸出上如何分辨「略過」與「失敗」。沒有就寫「無」。>

---

## 7. 建置與產物

**建置**

```bash
<指令>
```

**產物**

| 產物 | 用途 |
| :--- | :--- |
| `<專案名稱>-v1.0.0-Setup.exe` | <用途> |
| `<專案名稱>-v1.0.0-Portable.exe` | <用途> |

<!-- 此清單即本專案對外承諾的產物形式，每次發佈必須全數提供（見 RELEASE_RULES.md §3.1）。 -->

### 版本號

**單一來源**：`<檔案>` 的 `<欄位>`

| 位置 | 欄位 | 方式 |
| :--- | :--- | :--- |
| `<檔案>` | `<欄位>` | 手動（單一來源） |
| `<檔案>` | `<欄位>` | 手動 |
| 產物檔名 | — | 自動 |

標為「自動」的位置不得手動修改。發佈前依 `docs/rules/VERSION_RULES.md` §7 逐項核對。

---

## 8. 分支、commit 與 PR 慣例

- **主分支**：`main`
- **開分支**：<從哪開、命名規則>
- **commit 訊息**：<格式>

### 舊實作的保留

<!-- 尚未發生時寫「目前無」，但保留本節。 -->

| 分支 | 內容 | 保留原因 | 解除條件 |
| :--- | :--- | :--- | :--- |
| `legacy/<描述>` | <舊實作是什麼> | <為什麼留> | <什麼條件下可移除> |

舊實作以分支保留、不刪除；該分支不再接受新功能。

---

## 9. 安全與敏感資料

### 9.1 機密不進版控

| 項目 | 排除方式 | 本機該放哪 |
| :--- | :--- | :--- |
| <憑證／金鑰／密碼> | `.gitignore` 的 `<條目>` | <位置> |

### 9.2 權限最小化

| 要求的權限 | 為什麼需要 |
| :--- | :--- |
| <例：讀寫使用者選定的資料夾> | <理由> |

**刻意不要的權限**：<例：不開放完整檔案系統存取，因為…>

### 9.3 依賴來源與鎖檔

- 鎖檔：`<檔名>`，<進版控／不進版控>
- 安裝指令：`<指令>`（<為什麼用這個而非另一個>）

### 9.4 破壞性操作的保護

| 操作 | 影響的資料 | 可回復機制 |
| :--- | :--- | :--- |
| <例：覆寫使用者檔案> | <哪些檔> | <備份策略；是否覆蓋既有備份；如何還原> |

<!-- 簽章、SmartScreen、校驗碼屬發佈議題，寫在 docs/rules/RELEASE_RULES.md，不寫在此。 -->

---

## 10. 已知陷阱

<!-- 尚未踩到任何坑時，寫「目前無」，但保留本節。踩到就補一條。 -->

#### <一句話標題>

- **症狀**：<你會看到什麼。錯誤訊息寫原文，這是別人搜尋得到的唯一途徑。>
- **原因**：<為什麼會這樣>
- **處置**：<怎麼解決或避開>

---

## 相關文件

- 使用說明：[README.md](README.md)
- 變更紀錄：[CHANGELOG.md](CHANGELOG.md)
- 文件與發佈規範：[docs/rules/](docs/rules/)
