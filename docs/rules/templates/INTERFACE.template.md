# <專案名稱> 介面規格

> 這份文件記錄本專案介面的**實際數值與行為**。改動畫面時先看這裡，改完回頭更新這裡。
> 撰寫規則見 [docs/rules/INTERFACE_RULES.md](docs/rules/INTERFACE_RULES.md)。

---

## 1. 適用範圍與視窗尺寸

| 項目 | 值 |
| :--- | :--- |
| 畫面型態 | <例：單一視窗的桌面小工具 / 多面板工作站 / 網頁> |
| 預設尺寸 | <寬> × <高>（邏輯像素） |
| 最小尺寸 | <寬> × <高> |
| 可否縮放 | <可 / 不可> |

<!-- 尺寸一律以邏輯像素為準。高 DPI 下量測時，工具本身必須是 DPI 感知的，
     否則會量到實體像素而誤判版面溢出。 -->

---

## 2. 色票

沿用 [docs/rules/tokens.css](docs/rules/tokens.css) 的共用 token（**必須共用**，不得自訂）。

| Token | 色值 | 用在哪 |
| :--- | :--- | :--- |
| `--color-background` | `#131313` | <用途> |
| `--color-surface-container-low` | `#1b1b1c` | <用途> |
| `--color-primary-container` | `#007acc` | <用途> |
| … | … | … |

**本專案額外定義的色**：<沒有就寫「無」>

---

## 3. 字體與字級

字體堆疊沿用共用 token（**必須共用**）。字級表屬**建議共用**。

| 用途 | 字級 | 字重 | 行距 | 字體 |
| :--- | ---: | :--- | ---: | :--- |
| 應用程式標題 | 16px | bold | 1.2 | sans |
| 區塊標題 | 14px | medium | 1.4 | sans |
| 內文 / 按鈕 | 14px | normal | 1.5 | sans |
| 欄位標籤 | 12px | normal | 1.4 | sans |
| 次要說明 | 11px | normal | 1.5 | sans |
| 路徑 / 日誌 | 11px | normal | 1.6 | mono |

- 同一列的控制項字級必須相同。
- 不得使用表外的字級。
- 等寬字體只用於路徑、雜湊、日誌、程式碼。

---

## 4. 間距

| 名稱 | 值 | 用於 |
| :--- | ---: | :--- |
| 視窗外緣 | 20px | 內容距視窗四邊（上下左右一律相同） |
| 區塊間 | 12px | <哪些區塊之間> |
| 控制項間 | 8px | 同一列的控制項之間 |
| 標籤與欄位 | 6px | 欄位標籤與其輸入框之間 |
| 區塊內邊距 | 10px | <哪個區塊的內容距邊框> |

- **四邊外緣必須相等**，不得單獨加大某一邊。
- 不得使用表外的值；真的需要就先改這張表。

---

## 5. 控制項

### 尺寸

| 項目 | 值 |
| :--- | :--- |
| 標準高度 | 36px（輸入框、下拉、按鈕一律） |
| 緊湊高度 | 28px（工具列、表格內） |
| 圓角 | 6px |
| 水平內邊距 | 至少 12px |

同一列的控制項**必須等高、字級相同**，寬度可不同。

### 按鈕角色

| 角色 | 外觀 | 用於 |
| :--- | :--- | :--- |
| 主要 | 實心，`--color-primary-container` 底，無框 | <該畫面唯一的主要動作> |
| 次要 | 透明底，`--color-outline-variant` 描邊 | <其餘動作> |

每個畫面**最多一個**主要按鈕。按鈕文字用動詞。

### 按鈕狀態

| 狀態 | 表現 |
| :--- | :--- |
| hover | 主要提亮 10%；次要底色加深一階 |
| active | 不做位移，只加深 |
| disabled | 不透明度 40% + 禁止游標 |
| 進行中 | 改文字（如「執行中…」）並停用 |

---

## 6. 視覺效果

### 不透明度

只使用共用 token 的五檔（**必須共用**）：

| 檔位 | 值 | 用於 |
| :--- | ---: | :--- |
| 主要 | 100% | 主要文字、實心元件 |
| 次要 | 70% | 次要文字 |
| 提示 | 50% | 提示文字、placeholder |
| 停用 | 40% | 停用狀態 |
| 分隔 | 25% | 分隔線、邊框 |

### 光暈 / 模糊 / 漸層

| 效果 | 是否使用 | 用途與強度 |
| :--- | :--- | :--- |
| 光暈（box-shadow 發光） | <使用 / 未使用> | <用在哪、色值與模糊半徑> |
| 高斯模糊（backdrop-filter） | <使用 / 未使用> | <用在哪、模糊半徑> |
| 漸層 | <使用 / 未使用> | <用在哪、起訖色與方向> |

<!-- 沒用到就寫「未使用」，不要刪掉這張表——這樣才分得出「刻意不用」與「忘了寫」。 -->

層次一律靠 `surface` 色階表現。

---

## 7. 狀態與回饋

| 狀態 | 表現 |
| :--- | :--- |
| 焦點 | 2px `--color-primary` 外框、偏移 2px，走 `:focus-visible`（**必須共用**） |
| hover | <說明> |
| 停用 | 不透明度 40% + 禁止游標（**必須共用**） |
| 載入 / 進行中 | <說明> |
| 錯誤 | <說明> |

<!-- 焦點樣式不得省略：所有可以 Tab 走到的元件都必須看得見焦點。
     禁止只寫 outline:none 而不補替代樣式。 -->

### 日誌 / 訊息等級（若有）

等級名稱與配色屬**必須共用**：

| 等級 | 顏色 |
| :--- | :--- |
| `INFO` | `--color-on-surface-variant` |
| `WARN` | <琥珀色> |
| `ERROR` / `FATAL` | `--color-error` |
| `SUCCESS` / `DONE` | `--color-tertiary` |

---

## 8. 畫面結構

<!-- 版型屬「各自決定」，不得與其他專案互相套用。建議用 ASCII 示意，不用截圖。 -->

```text
<在這裡畫版型示意>
```

| 區塊 | 職責 |
| :--- | :--- |
| <區塊> | <做什麼> |

---

## 相關文件

- 使用說明：[README.md](README.md)
- 開發者文件：[DEVELOPER.md](DEVELOPER.md)
- 撰寫規則：[docs/rules/INTERFACE_RULES.md](docs/rules/INTERFACE_RULES.md)
