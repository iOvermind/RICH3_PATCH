# MAP.MKF 誤植修正：清掉三處錯誤的地點資料

給 **RICH3_PATCH**。這三處是《大富翁3》原版資料的誤植，會讓遊戲顯示錯誤的地名或把
不相干的格子算進同一個地點。修法都是把誤填的欄位歸零。

特徵碼與替換內容已經算好，**每一條都在整份 `MAP.MKF`（1,944,084 byte）裡驗證過只出現
一次**，可以直接用 `ReplaceMode::All` 而不會誤傷。

---

## 背景：這些數字是什麼

`MAP.MKF` 的每張地圖有一組 `#02 座標地點轉換表`，50×50 格、**每格 10 byte**，五個
小端 16 位元整數：

| 位移 | 欄位 |
| :--- | :--- |
| 0–1 | 路段編號（**名稱**由此而來：名稱索引 = 路段編號 + 0x54，去 `WORD.DAT` 查） |
| 2–3 | 地點形態（0=無 1=事件 2=道路 3=港口 4=車站 5=海港 6=機場 7=建地 8=租地 9=公司） |
| 4–5 | 實際作用地點 X |
| 6–7 | 實際作用地點 Y |
| 8–9 | 資訊編號（意義依形態而定） |

所以 `E0 00 00 00 00 00 00 00 51 00` = 路段 224、形態 0、作用 (0,0)、資訊 81。

---

## 三處誤植

### A. 大陸 (1,0) — 顯示成「中山南路」

最上緣第二格。形態是 `33`，而全部 7,500 格裡只有這一格超出 0–9 的範圍；路段編號 39
讓它顯示「中山南路」（名稱索引 39 + 0x54 = 123）。

連帶 (0,0)–(4,0) 整段都有殘留（(0,0) 帶資訊 47，(2,0)(3,0)(4,0) 帶作用座標 (2,4)），
但只有這一格會顯示名稱。台北與台灣的同一段是全零。

```
原始：路段 39、形態 33、作用 (2,4)、資訊 0
```

### B. 台北 (29,30) (29,31) (29,32) — 顯示成「新生北路」

九格共用「新生北路」這個名字：

```
(27,30) 建地 資訊81   (28,30) 道路 資訊156   (29,30) 形態0 資訊81
(27,31) 建地 資訊82   (28,31) 道路 資訊157   (29,31) 形態0 資訊82
(27,32) 建地 資訊83   (28,32) 道路 資訊158   (29,32) 形態0 資訊83
```

左邊六格（建地與它們的收費道路）帶這個名字是正常的。**右邊三格是建地 81/82/83 的
建築物圖**——資訊編號對得上，但它們不該帶路段編號，帶了就會在遊戲裡顯示名稱。

⚠ **只清路段編號（前 2 byte），不要動資訊編號。** 那個編號是這三格與地產的關聯。

### C. 大陸 (46,36) (47,36) — 被算進公園

大陸有兩個公園。(25,12) 是正常的 2×2；(46,36) 卻是 2×3：

```
(46,36) (47,36)  方塊 481 482  形態1 路段12 資訊249   ← 誤植
(46,37) (47,37)  方塊   0   1  形態1 路段12 資訊249
(46,38) (47,38)  方塊   2   3  形態1 路段12 資訊249
```

**多出來的上排用的方塊是 481 / 482，根本不是公園的圖**（真公園四格用方塊 0/1/2/3，
與 (25,12) 那個完全相同）。那兩格只是被誤填成形態 1／路段 12／資訊 249。

---

## 規則

加進 `src-tauri/src/patch/rich3.rs` 的 `patch_map_mkf()`，與現有的
「台北建國北路二層房過路費」放在同一個 `rules` 陣列即可。

```rust
// ── MAP.MKF 誤植修正 ───────────────────────────────────────────────
// 三處原版誤植，會讓遊戲顯示錯誤的地名或把不相干的格子算進同一個地點。
// #02 座標地點轉換表每格 10 byte：路段/形態/作用X/作用Y/資訊，皆為小端 u16。
// 每條特徵碼都在整份 MAP.MKF 裡驗證過只出現一次。

Rule::new(
    "大陸 (1,0) 清除誤植的路段39與形態33（顯示成中山南路）",
    &[("27 00 21 00 02 00 04 00 00 00",
       "00 00 00 00 00 00 00 00 00 00")],
),

// 台北那三格只清前 2 byte 的路段編號；資訊編號 81/82/83 是它們與建地的關聯，要留著。
Rule::new(
    "台北 (29,30) 清除誤植的路段224（顯示成新生北路）",
    &[("E0 00 00 00 00 00 00 00 51 00",
       "00 00 00 00 00 00 00 00 51 00")],
),
Rule::new(
    "台北 (29,31) 清除誤植的路段224（顯示成新生北路）",
    &[("E0 00 00 00 00 00 00 00 52 00",
       "00 00 00 00 00 00 00 00 52 00")],
),
Rule::new(
    "台北 (29,32) 清除誤植的路段224（顯示成新生北路）",
    &[("E0 00 00 00 00 00 00 00 53 00",
       "00 00 00 00 00 00 00 00 53 00")],
),

// ⚠ 大陸公園那兩格相鄰，必須寫成**一條**規則。
//   拆成兩條的話，第一條改完之後第二條的特徵碼就對不上了。
Rule::new(
    "大陸 (46,36)(47,36) 清除誤填成公園的欄位",
    &[("00 00 00 00 00 00 00 00 00 00 0C 00 01 00 00 00 00 00 F9 00 0C 00 01 00 00 00 00 00 F9 00 00 00 00 00 00 00 00 00 D2 00",
       "00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 D2 00")],
),
```

### 檔案位移（對照用，不是規則的一部分）

| 項目 | 位移 |
| :--- | :--- |
| 大陸 #02 起點 | `0x1D1C14` |
| 大陸 (1,0) | `0x1D1C1E` |
| 大陸 (46,36) | `0x1D6430` |
| 台北 #02 起點 | `0x7A37C` |
| 台北 (29,30) | `0x7DF36` |
| 台北 (29,31) | `0x7E12A` |
| 台北 (29,32) | `0x7E31E` |

---

## 怎麼驗

**改完後總共只該有 13 個 byte 不同**（A 改 4、B 每格 1 共 3、C 改 6）。跑完拿改過的
`MAP.MKF` 與 `.bak` 逐位元組比對，數字對不上就是有規則誤傷了別的地方。

進遊戲要確認的：

| 位置 | 改前 | 改後應該 |
| :--- | :--- | :--- |
| 大陸 (1,0) | 顯示「中山南路」 | 沒有名稱 |
| 台北 (29,30)–(29,32) | 顯示「新生北路」 | 沒有名稱 |
| 台北 (27,30)–(28,32) | 顯示「新生北路」 | **不變**，仍然顯示 |
| 台北建地 81/82/83 | 可買賣、收租正常 | **不變** |
| 大陸 (46,36)(47,36) | 屬於公園 | 不屬於公園 |
| 大陸 (46,37)–(47,38) | 公園 | **不變**，仍是公園 |

⚠ 重點在「**不變**」那幾列。這三處修的都是**多餘**的資料，正常運作的部分一格都不該受影響。

---

## 與 RICH3_EDITOR 的關係

編輯器載入時會跑結構檢查。**套用這個修正之後，大陸那條「出現未知的地點形態 33（1 格）」
的警告會消失**——那正好可以當作修正生效的證明，不必開十六進位編輯器。

編輯器目前已經在**顯示層**繞過 B 與 C（圖章不會做出橫跨三塊地的假區塊、公園圖章是正確的
2×2），但**資料本身沒動**，所以「地點」頁點大陸公園仍會看到 6 格。這個修正是從源頭處理。

背景與後續追蹤見 `RICH3_EDITOR/docs/OPEN_QUESTIONS.md` 的 Q2。
