## 各版本號更新意義與發佈時機

### 1. 主版本號 (MAJOR) — `X.0.0`
> **關鍵字：不向下相容（Breaking Changes）**

* **更新意義**：包含了任何可能破壞既有呼叫方式、架構或 API 的重大變更。升級此版本時，使用者或下游模組可能需要修改程式碼才能繼續正常運作。
* **觸發時機**：
  * 修改或刪除了既有的 API、函數簽名或資料結構。
  * 徹底重構底層架構，導致舊介面無法運作。
  * 移除已廢棄（Deprecated）的功能。
  * 產品由開發/測試階段進入第一個正式穩定版（從 `0.x.x` 跨越到 `1.0.0`）。

---

### 2. 次版本號 (MINOR) — `1.Y.0`
> **關鍵字：向下相容的新功能（New Features）**

* **更新意義**：加入了新功能或擴充了模組，但**完全向下相容**。既有程式碼直接升級後仍可正常運作，無需修改。
* **觸發時機**：
  * 新增新的 API、元件或參數（且預設值不影響原有行為）。
  * 標記某些 API 預計在未來廢棄（加入 Deprecated 警示）。
  * 顯著提升內部效能或改進處理邏輯（且輸出結果一致）。

---

### 3. 修訂號 (PATCH) — `1.0.Z`
> **關鍵字：向下相容的 Bug 修復（Bug Fixes）**

* **更新意義**：修復了既有的程式缺陷或安全性漏洞，**完全向下相容**。此版本不包含任何新功能或介面變更。
* **觸發時機**：
  * 修復程式崩潰（Crash）、記憶體漏失（Memory Leak）或邏輯錯誤。
  * 修正介面錯字、樣式小跑版。
  * 補強已知安全性漏洞。

---

## 快速對照與發佈決策表

| 變更類型 | 變更範例 | 版本號變動 | 向下相容？ |
| :--- | :--- | :---: | :---: |
| **修復 Bug** | 修正了計算公式遺漏負數處理的錯誤 | 1.0.0 → 1.0.1 | 🟢 是 |
| **新增功能** | 增加匯出 Excel 報表的功能 | 1.0.1 → 1.1.0 | 🟢 是 |
| **標記廢棄** | 宣告舊版認證 API 將於 2.0 移除 | 1.1.0 → 1.2.0 | 🟢 是 |
| **重構/破壞性變更** | 變更 API 傳入參數格式，舊版呼叫會報錯 | 1.2.0 → 2.0.0 | ❌ 否 |

---

## 特殊階段與預發佈標籤

1. **開發階段 (`0.y.z`)**：
   * `0.x.x` 代表初始開發階段，API 隨時可能改變，不受 SemVer 向下相容約束。
2. **預發佈版本 (Pre-release)**：
   * 可在 `PATCH` 後加上連字號與標籤，例如：
     * `1.0.0-alpha.1`（內測版）
     * `1.0.0-beta.2`（公測版）
     * `1.0.0-rc.1`（Release Candidate，發佈候選版）

---

## 專案 Changelog 範例模板

可在專案根目錄的 `CHANGELOG.md` 搭配使用：

```markdown
# Changelog

## [1.1.0] - 2026-08-04
### Added (新增)
- 新增使用者匯出 PDF 報表功能

### Fixed (修復)
- 修正搜尋欄位輸入特殊字元時頁面卡死的問題

## [1.0.1] - 2026-08-01
### Fixed (修復)
- 修正登入逾時未自動跳轉至登入頁面的漏洞

## [1.0.0] - 2026-07-20
### Added (新增)
- 第一個正式穩定版本發佈，提供完整核心功能